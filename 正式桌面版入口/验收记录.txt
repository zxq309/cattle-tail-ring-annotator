牛尾环九轴视频标注工具 - 正式桌面版 v7
验收日期：2026-07-31

唯一正式入口：
  【双击启动】牛尾环标注工具.bat
Python 入口：
  ..\launch_desktop.py → ultimate_v7_window.MainWindow
  （2026-07-31 由 v4 切换至 v7；v4 入口与旧文档已归档至
   ..\归档_v4入口_20260731\，可恢复）

真实回归文件：
  JSON：2026-07-16 10_23_08.json
  设备：546C50CA07EF
  样本：81,946
  曲线：11
  时长：1,799,900 ms
  静止合加速度：0.998 g
  视频：G:\hiv00000.mp4
  实际容器：MPEG-PS
  视频编码：HEVC
  时长：994,240 ms

实测指标：
  视频先开、JSON 后开
  初始化期间只点击一次，完成后自动播放
  500,000 ms 定位成功
  2.0x 倍速成功
  两处播放头误差：0.0 ms
  平滑播放头采样：45 次
  20 ms 最短区间、撤销与重做通过
  工程视频身份校验与事务回滚通过
  FFmpeg 仅使用 -c copy，输出仍为 HEVC
  正常关闭退出码：0

最终加固：
  100 ms 小幅定位使用 50 ms 严格确认容差
  切换视频会清除旧视频的待确认定位
  画面角标改成无共同覆盖时间时立即暂停
  工程回滚后核验播放器实际视频路径
  回滚视频失败时清空视频并阻止导出
  FFmpeg FailedToStart 同步或异步失败均恢复界面

旧版原生崩溃复查：
  用户截图时间：2026-07-31 13:20:25
  稳定媒体关闭层生成时间：2026-07-31 13:45:04
  当前正式 v4 生成时间：2026-07-31 14:55:05
  截图属于当前正式版生成前的旧构建。
  旧关闭链在该海康 PS/HEVC 上调用 libVLC stop 时可触发
  0xC0000005；当前关闭链已避开该危险调用。
  当前 v4 完成 18 个独立进程压力回归，退出码全部为 0。
  覆盖：打开即关、播放中关闭、20 次连续定位、25 次重复打开、
  1x/2x/4x 倍速，以及 RTX 3050 D3D11VA 可见窗口播放。

说明：
  默认直接调用 VLC 解码，不进行转码。
  FFmpeg 只在用户主动选择诊断回退时执行无损换封装。
  原始视频和原始 JSON 不会被修改。

v7 新增功能实测（codex 记录，2026-07-31 16:33）：
  悬停时间吸附到真实 JSON 样本，显示样本号、相对时间、
    记录时间和时间来源；采样间隙显示橙色警告，不展示假精度。
  暂停时九轴与视频可分别选择起点；按一次播放，待 HEVC
    定位确认后自动配对并同步播放。
  最大倍速提升至 10×，失败自动回退。
  真实 JSON：81,946 样本、218 个间隙；
    600003 ms 正确吸附为 600008 ms。
  真实 HEVC：10× 设置成功，1.5 秒推进 13.2 秒。
  v7 代码：..\ultimate_v7_window.py
    （DualAnchorPrecisionV2Mixin + v4 主体，MRO 叠加，不改 v4 代码）

v7 切换记录（Kimi 接续完成，2026-07-31 17:31）：
  codex 因审批额度限制未能写回正式入口；本次切换内容：
  1. v4 入口与两份旧文档归档至 ..\归档_v4入口_20260731\
  2. launch_desktop.py 导入由 ultimate_v4_window 改为 ultimate_v7_window
  3. anaconda 环境导入冒烟测试通过（IMPORT OK）
  4. 使用说明.md、验收记录.txt 已同步更新为 v7

v7.1 增量（Kimi，2026-07-31 17:50）：
  曲线左下角新增“时间尺度”选择器：1 分钟 / 5 分钟 / 10 分钟 /
    全部，以当前播放头为中心切换窗口；滚轮或拖动改窗口后自动
    显示“自定义”。解决全程视图数据过密看不清的问题。
  离屏功能测试 6 项全部通过（各档跨度、播放头居中、全部、
    自定义同步、自定义不动视图）。

v7.2 增量（Kimi，2026-07-31 18:00）：
  新增「泳道」显示开关：8 条标签泳道占 144 px，一键隐藏后
    九轴曲线占满图区（实测图区高度 318→462 px），再点恢复。
  离屏测试通过：开关、图区增高的绘制、时间尺度回归。

v7.3 增量（Kimi，2026-08-03）：
  标签体系升级：
  1. 新增三个标签（defaults.py 与 annotation_core.py 内嵌兜底表同步）：
     其他/other（键 8，状态兜底，最短 5s）、排便/defecation（键 9，
     事件，最短 8s，bout 文献均值约 38s）、甩尾/tail wagging
     （键 W，事件，最短 1s，高频 1-2 秒级）。快捷键机制
     QKeySequence 支持字母键，W 无冲突，已实配。
  2. 撒尿最短时长 2.0s → 8.0s（Williams 2022：排尿 bout 最短 8 s）。
  3. 事件新增「情境」字段 ctx 全链路贯通：
     annotation_core.Event 增 ctx 字段与 context 属性，to_dict 输出
     "ctx"、from_dict 兼容 "ctx"/"context"，工程保存/载入不丢；
     defaults.py 新增 CONTEXT_OPTIONS（抬尾：不明/排便/排尿/发情/
     临产；甩尾：不明/驱蝇/发情/临产；自定义标签回退为空列表）；
     标签面板置信度旁新增「情境」下拉；新建事件写入当前值，选中
     事件时判据/置信度/情境三下拉回显、改下拉即写回选中事件；
     事件表加第 8 列「情境」；events.csv 在 confidence 后新增
     context 列（原值输出，空串留空）；ctx 不强制、不校验取值；
     BORIS 导出不变。
  4. 连带修正：标签数由 8 增至 11，MAX_VISIBLE_LABELS 与泳道
     上限由 8 提至 16（否则含新标签的工程会被拒绝载入）。
  离屏测试（QT_QPA_PLATFORM=offscreen，anaconda python，
  真实实例化 ultimate_v7_window.MainWindow，真实合成 v2 九轴
  JSON 走完整 保存→解析→设备校验→载入 链路）：
  1. 标签表断言通过（defaults 与 annotation_core 内嵌表一致）
  2. 事件 ctx 新建 → 保存工程 → 重新载入 断言通过
  3. CSV 导出 context 列断言通过
  4. 事件表 8 列断言通过
  5. 选中事件回显 + 改下拉更新事件 断言通过
  PASSED
  文档同步：牛行为标注范围与操作性定义.md（状态表加「其他」、
    事件表加「排便」「甩尾」、撒尿改 ~8s、新增「情境子标签」节、
    映射表与校准清单同步、提示协议版本号 +1）、使用说明.md
    （快捷键清单与情境下拉说明）、本文件。
  注意：协议版本号应在下次发布时 +1；已打开的标注窗口需重启
    后才能使用新标签。
