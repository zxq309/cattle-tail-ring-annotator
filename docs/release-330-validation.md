# 3.3.0 功能实测记录

测试日期：2026-09-09～10。按“整理 → 标注保存 → 复核导出”执行，使用当前源码、随包私有运行库、隔离演示素材及本机真实九轴/录像。原始数据未移动或改写。客户截图中的失败工程在客户电脑，本机未直接验证其原件。

## 自动化回归

合并修复后的全量结果：724 passed，5 skipped，47.80秒。跳过项是源码环境下缺少可选事件模型运行条件的既有测试；五个随包模型另做真实运行。静态检查范围为 CI 使用的 ruff check cowmata_tailring tests，并检查 Git 差异空白；不宣称历史脚本、模型资源或第三方代码全部通过静态分析。

## 工程加载、播放与关闭

| 功能 | 实际结果 |
| --- | --- |
| 客户式 pending / 3.1旧索引 | 真实Qt、FFmpeg、RapidOCR；嵌套中文目录，录像开头2秒没有时间字幕。冷开首帧45.0秒，模拟旧索引迁移首帧41.4秒；自动选中视角03，未读九轴仍可见。 |
| 真实本机工程冷开 | 清点366份IMU、842份视频。首份九轴1.144秒，首个画面14.937秒，默认自动选中视角01；GUI最长间隔0.493秒。 |
| 已有索引重开 | 首份九轴1.680秒，首帧2.580秒。多实例测试同时运行时GUI最长间隔0.900秒，不能据此承诺所有机器秒开。 |
| 加载中取消 | 清点开始0.483秒请求取消，0.179秒关闭，未留下待加载元数据。正常保存退出约0.94～1.15秒。 |
| 视频异常 | 真实录像中发现一份缺失moov结构，FFprobe独立确认；保留原件并标异常，其他录像正常载入。没有把842份录像都算作已解码验证。 |
| 单视角播放和拖动 | 三个真实相机分别执行1、2、4倍速、远跳/近跳/连续拖动。实际倍率范围0.962～0.996、1.988～2.004、3.966～4.017；暂停远跳0.292～0.745秒，连续拖动最终帧0.435～0.618秒，无超时或非请求跳转。 |
| 1～8路播放 | 合成两段视频/视角，1～8路×1/2/4倍速共24种组合均通过，八路均越过文件边界；最慢全部就绪2.391秒，峰值内存约962MB。此项为640×360素材，不冒充八路4K吞吐。 |
| 八路高分辨率历史回看 | 实际HEVC牛舍短片构造8路、两段/路；完整标签和片段重开、跨文件、终点和播放器数量通过。改为分次显示原生视频窗口后，GUI最长间隔由1.364秒降至0.730秒。重复短片仅用于负载测试。 |
| 时间显示 | 波形、位置、已确认标注列表和历史回看采用同一时钟映射；窄窗口减少刻度密度。未建立参考时钟时明确显示相对时间，内部样本坐标不改。 |

## 整理、标注与成果

| 功能 | 实际结果 |
| --- | --- |
| 审查和剪切 | 真实QProcess审查、预览、同卷重命名移动、源文件身份核对、完成索引；单设备、单日、多日和嵌套输入回归通过。演示整链移动6项约1.35秒，有效5项进入工程、1项杂物隔离。 |
| 占用与恢复 | 实际Windows工程锁、独立子进程互斥、跨盘E:→C:拦截、半行日志恢复、旧SQLite和失败续接均验证；跨盘不自动复制。 |
| 命名和日期复用 | 完整设备号-数字耳标-字母数字记号；前导零和记号大小写保留。乱序名称拦截但有效九轴原件保留；同设备跨日期换牛分别保存、重开和导出。 |
| 七类全链 | 正常健康、发情、孕早期、孕中期、孕晚期、产犊、疫病：归类、草稿、确认、撤销、重开、回传及各输出保留类别与身份。分类不限制行为标签。 |
| 保存、修改、恢复 | 实际开始/结束动作、保存两路证据、切记录自动保存/恢复、完成与退出提示、修改标签和重开。真实Windows短暂读句柄占用触发替换失败后恢复；永久写入失败保留内存、旧文件及备份，重试结束动作不重复添加。 |
| 完整/片段/训练导出 | 实际写盘并重开。全份真实九轴179366样本保留原始数据；片段200样本保留1005毫秒起点。事件CSV、逐样本CSV、BORIS、参考证据CSV、训练元数据和全部人工成果逐项检查。 |
| 大份回收与导出 | 合成1小时50Hz、180001样本、58标签；回收1.001秒/GUI最长0.538秒，训练导出1.492秒/0.384秒；七份输出，原件哈希不变。 |
| 多视角证据与归档 | 实际提取8张2560×1440证据图，最大帧误差35毫秒，耗时4.894秒；16文件归档SHA核验、改路径重连、断开原录像后图片放大通过。 |
| 五类算法 | 私有模型运行库分别运行起立、卧倒、排尿、抬尾、甩尾；真实九轴输出候选0/2/1/3/12。模型候选检查19项、结果定位、全量保存重开通过。另一静稳合成输入测试取消0.248秒退出；不等同算法准确率验证。 |
| 图文手册 | 当前Qt实际执行整链并截图，32页/38张图，每页3步，PDF书签32项。全页渲染检查，关于和帮助共用同一PDF。详见[教程验证范围](manual-330-validation.md)。 |

## 更新与安装验收

启动检查的“已是最新”“存在更新”“网络失败”三分支用实际Qt窗口验证，网络和安装启动边界受控替代；需更新或检查失败时正常工作台均未开放。损坏下载重试、回滚失败保护、真实独立更新进程互斥另有回归。

独立旧版3.2.1更新器已实际执行3.3候选安装器的解包、全文件哈希、私有Python/Qt导入、目录替换与旧文件清理，新启动器版本为3.3.0.0。此项注册和重启边界替代，未改现用安装；最终安装器另执行真实注册、新装、快捷方式和卸载检查。第一次升级仍由旧更新器执行，新互斥/恢复规则从3.3安装后生效。

发行前固定代码和手册，检查安装包SHA、包内清单、新装/卸载、旧更新器迁移及GitHub四组CI。**最终安装包的机器可读验收记录随同版本附件发布为 COWMATA-3.3.0-Delivery-Checks.json**；它在最终EXE生成后记录哈希和实测结果，避免文档内嵌自身安装包哈希造成循环。安装器、更新描述文件、SHA与验收记录一致后才公开最新版。

## 证据与范围

本机原始记录存放于隔离的 audit-330/：bundle-real-cold、bundle-real-reopen-02、bundle-real-cancel、playback-summary.json、bundle-multiview/summary.json、bundle-annotation-new/、full-suite-final.xml，以及独立迁移/新装报告。这些不是客户工程废料，不写入数据资源目录。

OCR、机械盘、网盘和显卡影响加载与播放速度，不能保证任意素材秒开。未穷举强行断电及所有硬件组合；历史外置Torch稠密预测导出不属于当前验收承诺。软件中的采集类别是人工分组，未接入的算法仍禁用；模型运行成功不代表科学有效性或诊断结论。调用链与已知边界见[全流程代码审查](code-audit-330.md)。
