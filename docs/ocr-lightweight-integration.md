# OCR 轻量改进：20260907

本版采用独立 OCR 任务冻结的 `candidate_v2`，不是第一轮 candidate。原始候选 `ocr.py` SHA-256：`d77ad81935ddd36701321ba7608d221a988fa2d708bcfd4e9d3f60a5c4d2657e`。不增加模型、依赖或在线下载；继续使用私有 RapidOCR 3.9.2 + PP-OCRv6 medium。

## 算法与证据边界

- 普通成功路径保留；失败后，有限动态范围图像尝试彩色/灰度范围拉伸，或对有中文日期线索的缩小 16:9 图像恢复常见尺寸。
- 两路增强结果成功且完整时间戳一致才接受，仍标为中等置信度、保存重试证据。
- 小字放大后出现有效读数冲突则拒识；专用日期识别出现不同年份/日期候选时不以多数票硬猜。
- 集成修正：已缓存的专用版式也执行冲突拒识，不回落到可能再次接受冲突读数的普通路径。
- 原分辨率 OCR、多帧时间推进、PTS 跳变/缺口核查继续保留。单帧成功或增强两路一致不构成真值保证。

独立任务报告的冻结回归：816 项确定真值正样本整串正确 706→731（86.52%→89.58%）；25 项拒识救回，两项已确认错认转为拒识，原正确样本无退步；64 项负样本未新增误报。236 项独立真实帧匹配结果不变，保留原标签争议。

这些是同一冻结集上的针对性改进回归，不是新的盲测或生产准确率承诺。合成/退化变体高度相关；拒识计入正样本分母，不能将错认转拒识计作正确。困难样本最多增加两轮 OCR，处理时间可能增加。

## 索引与人工标注

算法签名：`rapidocr-3.9.2/PP-OCRv6_medium/osd-light-v2-20260907`。打开旧工程时，旧自动 OCR 索引排队复核；源文件身份、人工标签、人工校准文件不删除。已明确人工确认的双锚点保持优先。未变化且已用本算法完成的素材复用索引，不每次全量重算。

复核中或失败的素材不冒充已校验时间映射；可以人工框选时间戳、换邻帧或录入实际读数。失败不代表没有录像，也不代表没有行为事件。

## 集成验证

单元测试 `tests/test_ocr_lightweight.py` 覆盖普通成功不增强、双路一致门槛、缩放后的原图尺寸/归一化 ROI、小字冲突、缓存版式日期冲突，以及索引升级保留人工标注。

实际图像回归、最终包离线 OCR、多视频/模型/导出测试的本次结果以 Release 验收记录为准，不用独立单帧评测替代桌面程序验收。
