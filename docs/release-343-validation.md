# COWMATA 3.4.3 验证记录

新增回归先复现失败再修复，覆盖：分辨率/OSD 角落造成同路拆分、旧名称选择与相机映射恢复、未确认草稿对齐后移位及撤销、三种状态新建和编辑互斥、混合标签排序且保留选择、核验片段与文件总时长区别、单点和双点核验浏览范围、无录像时清空旧计时、相机校准下仍显示文件秒数、进度滑块取整、人工保存与索引发布竞争、标准 MP4 不覆盖原件。

Windows 实际窗口已实测：单个人工读数的保存、保存后真实解码出画面、一条稳定视角、完整文件时长、草稿排序、手动校准后九轴位置不变、保存坐标、状态新建及边界编辑拦截。使用隔离的生成视频和生成九轴，不作为研究真值。2026-09-04 客户工程未提供本机完整路径，不能声称已在该原工程验证。

真实视频复核：256 MiB 的原始 Hikvision PS 录像内部连续视频为 973.631 秒、12181 帧；首尾实际画面与流内时间复核。标准 MP4 副本为973.631秒、12181个视频包，视频未重编码，原始内容校验值保持不变。文件末尾异常时间信息会导致通用播放器误报长时长，整理改名没有重编码或拉长原录像。

末段 OSD 部分数字为白色且背景复杂，自动 OCR 未完整通过；保留该限制，没有把识别失败当成自动核验成功。交付副本只含视频画面，原件音频和私有数据包没有删除。

最终本地回归、GitHub CI、完整便携包离线运行、安装及更新链路检查结果见 COWMATA-3.4.3-Delivery-Checks.json。
