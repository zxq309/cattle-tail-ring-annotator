# 牛尾环模型辅助标注整合版

本目录是独立工具副本，不会修改原始九轴 JSON、视频、训练数据或训练结果。

## 启动

双击 `正式桌面版入口\【双击启动】模型辅助标注工具.bat`。首次使用或选择新版因果 checkpoint 前，先运行 `【安装依赖】模型辅助标注工具.bat`。

## 当前算法口径

工具已经按 2026-08-14 方案升级为“持续姿态 + 行走子状态 + 独立事件”输出：

1. 每段采集以已知的“初始站立”启动站卧状态机；姿态证据、起立/卧倒概率、迟滞和最短驻留共同决定连续站/卧区间。
2. 行走作为直立状态中的独立运动子状态计算；导入现有互斥标注表时，行走覆盖对应的站立区间。
3. v4 协议已删除采食和其他标签。模型不生成采食建议；旧权重的采食概率只作为直立证据。
4. 起立、卧倒、排尿是正式辅助候选；排便、抬尾、甩尾是研究性扩样候选，默认不勾选。
5. 努责及分娩过程六项标签目前只支持人工标注，现有模型不会自动生成这些候选。
6. 工具可自动识别三类监督权重：目录内默认的 `HierarchicalMSResTCN`、`03_训练结果/models_codex/loco_v1` 与 `loco_v2_domain_robust` 下的历史 `MultiTaskResTCN`，以及训练代码生成的新版 `CausalMultiTaskTCN` checkpoint。
7. `models_codex/ssl_v1` 是自监督表征权重，只有编码器、没有行为分类头，不能直接用于自动标注；工具会在选择时明确拦截。

## 模型辅助流程

1. 打开真实九轴 JSON，并按需要打开、校准同步视频。
2. 点击“模型辅助预测”。默认使用 `models\production\best_model.pt`；也可用“选择模型…”选择 `models_codex` 下任一 `loco_v1/loco_v2_domain_robust` 的 `best.pt`，或兼容的新版因果 checkpoint。
3. 在“复核模型建议”窗口检查候选级别、置信度和提示。点击“平均置信度”或“最高置信度”表头可切换升序/降序；双击一行可定位到开始时间。
4. 勾选可信建议并导入。导入项带有待复核状态、模型/算法版本、来源头、阈值、候选级别和置信度。
5. 根据视频和曲线修正边界或删除误报；确认后在事件表选中该条并点击“确认建议已复核”。
6. 按原流程保存工程、导出 CSV/训练样本。未经人工确认的候选不得直接作为金标准。

## 去重与缓存

- 完全相同的建议按毫秒指纹去重；不同模型造成的小幅边界漂移按同类区间 IoU 去重。
- 身体行为建议与现有人工身体行为冲突时默认不勾选，导入时再次校验。
- 预测摘要按“源数据文件版本 + 模型文件版本 + 算法版本”分别缓存，切换模型不会覆盖旧摘要。
- 校准指标中的 `NaN/Inf` 会安全写为 JSON `null`，避免摘要缓存静默失败。

## 模型边界

默认 `best_model.pt` 的主体行为 macro-F1 约 0.943 是验证集估计，不是独立跨牛测试结果。当前交付的 `03_训练结果` 也明确属于历史固定中心窗基线；新版因果模型需要另行完成训练后再通过“选择模型…”接入。

新牛、新设备、新牧场必须人工复核。因果 checkpoint 若未包含目标设备的逐会话校准，工具会使用 4096/32/1000 标准灵敏度并在复核窗口明确警告；这种结果不得直接作为金标准。

`models_codex` 的 LOCO 权重采用 5.12 秒固定中心窗，含约 2.56 秒未来信息，只适合当前离线标注工具，不是在线因果模型。不同 fold 的留出牛和阈值不同；候选结果必须结合权重对应的 `report.json` 解释。验证集产生的极低 `TAIL_WAGGING` 阈值会在工具侧提升到 0.50 安全下限，避免每小时产生大量不可用候选。
