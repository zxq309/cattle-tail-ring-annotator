# 牛尾环模型辅助标注整合版

本目录是独立工具副本，不会修改原始九轴 JSON、视频、训练数据或训练结果。

## 启动

双击 `正式桌面版入口\【双击启动】模型辅助标注工具.bat`。首次使用或选择新版因果 checkpoint 前，先运行 `【安装依赖】模型辅助标注工具.bat`。

## 当前算法口径

工具已与 `牛尾环IMU_20260815_整理包` 的新版因果预测接口对齐：

1. 默认使用 `复现实验/development_all/fold_0_None_20260815_111619/best.pt` 的 `CausalMultiTaskTCN`。
2. 姿态头输出 `UPRIGHT/LYING`，行走头作为直立状态中的独立子状态；工具按接口规则生成站立、躺卧、行走区间，仅过滤不足 1 秒的孤立闪烁。
3. 六个事件严格使用 checkpoint 阈值和新版 `predict_continuous.py` 的分类别最短时长、合并间隔规则。
4. 起立、卧倒、排尿是正式辅助候选；排便、抬尾、甩尾是研究性扩样候选，默认不勾选。
5. 采食和其他不再作为标签；努责及分娩过程六项标签继续只支持人工标注。
6. 模型选择窗口只接受新版 `CausalMultiTaskTCN`。旧固定中心窗权重仍保留在目录中用于代码回滚，但不再通过界面运行。
7. 每个完整会话只清空一次 TCN 状态；分钟数据边界不会重复触发模型冷启动。
8. 优先读取整理包中训练时校准的 `features.npy`。没有匹配缓存、且 checkpoint 未内嵌设备校准时，工具会停止正式预测，不会静默使用默认灵敏度。

## 模型辅助流程

1. 打开真实九轴 JSON，并按需要打开、校准同步视频。
2. 在顶部“模型辅助”下拉菜单中点击“开始模型预测”。默认使用整理包推荐的新版因果 checkpoint；也可在同一菜单中用“模型设置…”选择其他 `CausalMultiTaskTCN` checkpoint。
3. 在“复核模型建议”窗口检查候选级别、置信度和提示。选择一行后点击“修改选中建议”，可在导入前修改标签、开始时间和结束时间；“开始/结束 = 当前播放位置”可直接使用刚刚定位确认的画面时间。
4. 勾选可信建议并导入。导入项带有待复核状态、模型/算法版本、来源头、阈值、候选级别和置信度。
5. 导入后在事件表选择预测事件，工具会自动聚焦该时段，并把预测区间以半透明色带覆盖在全部真实九轴波形上。直接拖动色带左右竖线即可校准开始/结束边界。
6. `Ctrl+E`、“调整预测区间”或双击预测事件可随时重新聚焦。“修改标签/时间…”用于更换标签或精确输入时间。拖动自动吸附真实九轴样本；身体行为冲突或同类高度重复时会撤回。
7. 波形拖动和输入框修改都会保留原始模型标签、原始边界和人工调整历史，并把状态重新设为待复核；操作支持撤销/重做。
8. 根据视频和曲线完成修正或删除误报；确认后在事件表选中该条并点击“确认已复核”。
9. 按原流程保存工程、导出 CSV/训练样本。未经人工确认的候选不得直接作为金标准。

## 去重与缓存

- 完全相同的建议按毫秒指纹去重；不同模型造成的小幅边界漂移按同类区间 IoU 去重。
- 身体行为建议与现有人工身体行为冲突时默认不勾选，导入时再次校验。
- 预测摘要按“源数据文件版本 + 模型文件版本 + 算法版本”分别缓存，切换模型不会覆盖旧摘要。
- 校准指标中的 `NaN/Inf` 会安全写为 JSON `null`，避免摘要缓存静默失败。

## 模型边界

默认模型是全部 5 头牛参与训练的开发扩样模型，综合验证分约 0.7357。它适合辅助扫描和人工扩样，但不是独立跨牛测试结论。

当前 `D:\Label\7.29-8.3\九轴数据` 的 190 个 JSON 均能匹配整理包训练缓存。未来新增会话如果没有缓存，需要先提供校准清单并按新版流程生成 `features.npy`。

新牛、新设备、新牧场的结果仍必须人工复核。甩尾、排便、抬尾样本稀少，只能作为研究性提示；未经人工确认的候选不得进入金标准。
