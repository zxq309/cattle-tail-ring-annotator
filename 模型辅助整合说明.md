# 牛尾环模型辅助标注整合版

本目录是独立工具副本，不会修改原始九轴 JSON、视频、训练数据或训练结果。

## 启动

双击 `正式桌面版入口\【双击启动】模型辅助标注工具.bat`。首次使用或选择新版因果 checkpoint 前，先运行 `【安装依赖】模型辅助标注工具.bat`。

## 当前算法口径

工具已经按 2026-08-14 方案升级为“持续姿态 + 行走子状态 + 独立事件”输出：

1. 每段采集以已知的“初始站立”启动站卧状态机；姿态证据、起立/卧倒概率、迟滞和最短驻留共同决定连续站/卧区间。
2. 行走作为直立状态中的独立运动子状态计算；导入现有互斥标注表时，行走覆盖对应的站立区间。
3. 模型不再生成采食建议。旧权重的采食概率只作为直立证据；人工仍可按原协议标注采食，以保留历史兼容。
4. 起立、卧倒、排尿是正式辅助候选；排便、抬尾、甩尾是研究性扩样候选，默认不勾选。
5. 工具可自动识别三类监督权重：目录内默认的 `HierarchicalMSResTCN`、`03_训练结果/models_codex/loco_v1` 与 `loco_v2_domain_robust` 下的历史 `MultiTaskResTCN`，以及训练代码生成的新版 `CausalMultiTaskTCN` checkpoint。
6. `models_codex/ssl_v1` 是自监督表征权重，只有编码器、没有行为分类头，不能直接用于自动标注；工具会在选择时明确拦截。

## 模型辅助流程

1. 打开真实九轴 JSON，点击“选择视频目录…”。普通视频按文件名时间与实际时长建立索引；海康裸录像目录则读取画面左上角 OSD 日期时间。索引过程只读，不修改原录像。
2. 点击“模型辅助预测”。默认使用 `models\production\best_model.pt`；也可用“选择模型…”选择 `models_codex` 下任一 `loco_v1/loco_v2_domain_robust` 的 `best.pt`，或兼容的新版因果 checkpoint。
3. 在“复核模型建议”窗口检查候选级别、置信度和提示。点击“平均置信度”或“最高置信度”表头可切换升序/降序；双击一行或点击“打开对应视频并定位”，工具会打开覆盖事件时间的视频，并跳到事件前 5 秒。
4. 勾选可信建议并导入。导入项带有待复核状态、模型/算法版本、来源头、阈值、候选级别和置信度。
5. 导入后在事件表选择待复核建议，点击工具栏“定位建议视频”可再次自动打开并定位；根据视频和曲线修正边界或删除误报，确认后点击“确认建议已复核”。
6. 按原流程保存工程、导出 CSV/训练样本。未经人工确认的候选不得直接作为金标准。

自动匹配只采用精确时间覆盖，不会猜测没有时间依据的视频。若多个视频同时覆盖事件，工具要求人工选择；若文件名没有可识别的日期时间、视频时长读取失败或目录中没有覆盖该时间的录像，则保留原有“打开视频”和人工校准流程。视频目录只保存在本机设置中，不改动原视频、九轴 JSON 和训练数据。

## 海康裸录像目录

类似 `海康1` 的目录即使只有 `hiv00000.mp4` 一类固定大小录像块、没有 `index00.bin` 等海康索引文件，也可以直接选择：

- 工具不按文件编号、修改时间或媒体 PTS 猜测真实时间，而是逐块识别画面左上角的 OSD 日期时间。
- 同一目录里的不同分辨率/通道分别索引；首次识别完成后可在工具栏设置固定复核通道，也可每次匹配时人工选择。
- 录像无需连续。每个真实连续片段独立建立起止时间，片段之间的空档不会被当成有录像；事件落在空档时会明确提示没有匹配视频。
- 全零预分配块会自动跳过；OSD 时间无法可靠识别、或时间明显倒退的片段不会写入错误索引。
- 首次扫描需要抽帧和 OCR，451 个 256MB 录像块预计约需 20–30 分钟，实际取决于磁盘和 CPU。每完成 5 块会保存缓存；点击“取消（保留已完成缓存）”后，下次选择同一目录会从未完成部分继续。
- 后续扫描会按文件大小和修改时间复用缓存，通常会明显加快。录像文件发生变化时，对应块会自动重新识别。

## 去重与缓存

- 完全相同的建议按毫秒指纹去重；不同模型造成的小幅边界漂移按同类区间 IoU 去重。
- 身体行为建议与现有人工身体行为冲突时默认不勾选，导入时再次校验。
- 预测摘要按“源数据文件版本 + 模型文件版本 + 算法版本”分别缓存，切换模型不会覆盖旧摘要。
- 校准指标中的 `NaN/Inf` 会安全写为 JSON `null`，避免摘要缓存静默失败。

## 模型边界

默认 `best_model.pt` 的主体行为 macro-F1 约 0.943 是验证集估计，不是独立跨牛测试结果。当前交付的 `03_训练结果` 也明确属于历史固定中心窗基线；新版因果模型需要另行完成训练后再通过“选择模型…”接入。

新牛、新设备、新牧场必须人工复核。因果 checkpoint 若未包含目标设备的逐会话校准，工具会使用 4096/32/1000 标准灵敏度并在复核窗口明确警告；这种结果不得直接作为金标准。

`models_codex` 的 LOCO 权重采用 5.12 秒固定中心窗，含约 2.56 秒未来信息，只适合当前离线标注工具，不是在线因果模型。不同 fold 的留出牛和阈值不同；候选结果必须结合权重对应的 `report.json` 解释。验证集产生的极低 `TAIL_WAGGING` 阈值会在工具侧提升到 0.50 安全下限，避免每小时产生大量不可用候选。
