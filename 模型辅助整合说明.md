# 牛尾环 20260816 模型辅助标注整合版

本工具只读取原始九轴 JSON、视频和模型，不修改训练数据或训练结果。所有模型输出必须经过人工复核后才能进入标注表。

## 启动与依赖

双击 `正式桌面版入口\【双击启动】模型辅助标注工具.bat`。首次使用 20260816 模型前运行 `【安装依赖】模型辅助标注工具.bat`；GBDT 序列化文件要求 `xgboost==3.2.0`。

## 20260816 模型包

“模型辅助 → 模型包设置…”选择一个目录。目录契约如下：

- `gbdt_full.joblib`：必需。提供六类事件，并在深度模型未完成时提供姿态/行走回退。
- `best.pt`：可选。训练完成后直接放入同一目录，必须是 `OfflineMultiTaskTCN`。工具下次预测会自动使用它负责站立、躺卧、行走，不需要重新设置。
- `inference_config.json`：可选。保存正式分类别阈值、后处理调整和新增设备校准；格式参考 `model_runtime/inference_config.example.json`。

默认目录是 `预测\20260816\复现实验\final_model`。当前只有 GBDT 时工具仍可正常预测，并在复核窗口明确显示“等待 best.pt”；不会把 GBDT 回退伪装成已完成的深度模型。

## 算法与标签口径

1. 深度模型负责互斥姿态 `UPRIGHT/LYING` 和独立的行走概率；六类事件始终使用 104 维离线手工特征 GBDT。
2. 深度模型未就绪时，`gbdt_full.joblib` 中的 `POSTURE_LYING/WALKING` 自动接管身体行为。
3. 姿态通过站立/躺卧状态机稳定切换；行走只叠加在直立状态上，最终输出互斥的站立、躺卧、行走区间。
4. 六类事件为抬尾、甩尾、起立、卧倒、排尿、排便，使用各自的最短时长和合并间隔；不再用一个统一规则处理。
5. 任何滤波、深度上下文、状态机和事件区间都不会跨越大于 750 ms 的采集断点。断续视频不会改变九轴预测时间。
6. 未提供正式 `inference_config.json` 时，事件阈值按新版指南暂用 0.5；工具会在复核窗口提示这一状态。
7. 排便、抬尾、甩尾仍按研究性候选处理，默认不勾选；所有候选都必须结合视频人工确认。
8. 采食和其他不作为模型标签；努责和分娩节点继续只支持人工标注。

## 任意原始 JSON

预测入口直接接收工具当前打开的 V2 原始 JSON，不要求提前生成 `features.npy`，也不要求数据已经存在于训练缓存。

处理步骤与训练一致：恢复 22 字节帧相位、识别时间复位/缺口、逐段 50 Hz 重采样、磁力计坐标旋转、13 通道物理量、逐会话安装姿态自校准、104 维离线特征和 2 Hz 决策点。

训练资料未附原校准清单。工具已通过七个设备的原始 JSON 与训练缓存逐帧对照，内置其固定偏置：

- `546C50CA01F1`
- `546C50CA07D0`
- `546C50CA07DE`
- `546C50CA07F2`
- `546C50CA07F8`
- `546C50CA090B`
- `546C50CA090F`

这些设备的未来新会话可直接预测。全新 MAC 必须先把实际 `acc_bias_counts` 和 `gyro_bias_counts` 写入模型目录的 `inference_config.json`；缺少校准时工具会明确停止，不会静默使用零偏置。

## 人工复核流程

1. 打开真实九轴 JSON，并按需要打开、校准同步视频。
2. 点击顶部“模型辅助 → 开始模型预测”。
3. 在复核窗口按置信度排序、定位视频，必要时先修改标签或起止时间。
4. 只勾选可信建议并导入；导入项统一标记为“待人工复核”。
5. 在事件表选择预测项后，可在真实九轴波形上拖动左右边界；`Ctrl+E` 可重新聚焦。
6. 根据视频修正或删除误报，再点击“确认已复核”。
7. 按原流程保存工程和导出训练样本。未经人工确认的候选不得作为金标准。

预测记录会保存原始模型目录、模型状态、GBDT/深度权重哈希、算法版本、阈值、来源分支、置信度和人工调整历史。添加或替换 `best.pt` 后，模型指纹会变化，预测摘要不会覆盖旧结果。

## 回滚

迁移前 Git 标签为 `before-20260816-hybrid-interface`。旧 20260815 因果运行代码仍保留在 `model_runtime/imu_behavior`，但当前界面的默认流程只选择 20260816 模型包。
